00) Неделя 00 — как работает обычный компилятор  
   Инженерная ветка: изучи базовую архитектуру классического компилятора — frontend (лексер, парсер, AST, типизация), IR (оптимизации, CFG), backend (кодоген). Пойми смысл “цепочки преобразований”: исходный код → промежуточное представление → результат.  
   Теоретическая ветка: отсутствует (мы пока не вводим категории). Цель — увидеть механику обычных компиляторов.  
   Репо: создай docs/compiler_basics.md (1–2 страницы: frontend, IR, backend, примеры диаграмм).  
   Нетворкинг: пока без активности, просто читаем и формируем общее представление.

0) Неделя 0 — контекст проекта и первые категории  
   Инженерная ветка: создаём репозиторий toy-ccc-compiler (pkg/, docs/, go.mod, README.md). Нарисуй схему Term → IR → Sem как аналог фронтенд → IR → бэкенд.  
   Теоретическая ветка: введение в категории (Mac Lane главы I–II). Что такое категория, морфизм, композиция, функтор, естественное преобразование. Нарисуй диаграммы id/compose и F(f∘g)=F(f)∘F(g).  
   Репо: docs/context.md (с диаграммой Term→IR→Sem), docs/ccc_notes.md (основные определения).  
   Нетворкинг: молчим, готовим внутренние заметки, без постов.

1) Неделя 1 — Cartesian Closed Categories (CCC) как база λ→  
   Инженерная ветка: изучи раздел STLC (Pierce, главы 9–12); зафиксируй типы (1, ×, →) и стрелки (id, ∘, π₁, π₂, ⟨–,–⟩, curry, uncurry, eval). Исправь Unit: ∀A ∃! !A:A→1, закон единственности f=!A.  
   Теоретическая ветка: CCC как структура с внутренним Hom; универсальные свойства для ×, →, 1; диаграммы для curry/eval и их связь с экспонентами.  
   Репо: docs/ccc_notes.md, диаграммы в Graphviz.  
   Нетворкинг: тихо, никакой активности.

2) Неделя 2 — Term (источник) и типизация  
   Инженерная ветка: определи грамматику Term без рекурсии (var, λ, app, unit, pair, fst, snd); введи контексты Γ, правила ослабления и подъёма индексов (lift/shift для de Bruijn); реализуй pkg/term (ast.go, subst.go, tests/term_subst_test.go); добавь pkg/types с total type-check (Γ⊢M:A).  
   Теоретическая ветка: адъюнкции (Mac Lane IV.1): unit/counit, треугольные тождества; связь λ-абстракции и экспонент как случая адъюнкции (– × A) ⊣ (–)^A для фиксированного A.  
   Репо: pkg/term/, pkg/types/, docs/adjunctions.md.  
   Нетворкинг: сохраняем молчание, готовим диаграммы адъюнкций.

3) Неделя 3 — IR как free-CCC и NbE  
   Инженерная ветка: определи примитивы IR (1, ×, →, id, ∘, ⟨–,–⟩, πᵢ, curry, uncurry, eval, !); пропиши полный набор β/η-законов и изо curry/uncurry; реализуй NbE (env/closures, reflect/reify) с формой НФ = β-нормальная, η-длинная; докажи canonicity (NF существует и единственна mod α).  
   Теоретическая ветка: free-CCC — левый сопряжённый к forgetful-функтору из CCC в множества генераторов; конструктивно реализуется как левая Кан-экстенсия с факторизацией по уравнениям. Параллельно изучи монады (Mac Lane VI.1–2): адъюнкция → монада (T=GF); как NbE реализует монадическую семантику; нарисуй, где в IR угадывается монада термов.  
   Репо: pkg/ir/, pkg/nbe/, docs/{ir_laws.md,nf_notes.md,monads.md}.  
   Нетворкинг: без постов, сосредоточься на реализации NbE.

4) Неделя 4 — компилятор как функтор F: Term→IR  
   Инженерная ветка: реализуй F(Term)→IRNode; формулируй и запиши теорему: “если M ≡βη N, то F(M) ≡IR F(N)” (индукция по ≡βη); добавь тесты на критические пары и проверку сохранения id, ∘, ×, →.  
   Теоретическая ветка: лемма Йонеды (Mac Lane II.5): Hom(–,A)⇒F ≅ F(A); Йонеда даёт представимость наблюдений, служит интуицией к инвариантам, но не заменяет доказательство soundness.  
   Репо: pkg/functor/, docs/invariants.md, docs/yoneda.md.  
   Нетворкинг: готовься к первому внутреннему посту, но ещё не публикуй.

5) Неделя 5 — Sem = FinSet  
   Инженерная ветка: реализуй Sem = FinSet с eval:(B^A×A)→B, β/η для ×, →; экстенсиональная проверка (по таблицам для конечных типов); tests/sem_commute_test.go; докажи soundness ⟦F(M)⟧ = ⟦M⟧ по индукции.  
   Теоретическая ветка: пределы/копределы (Mac Lane V); CCC = категория с внутренним Hom; FinSet как частный случай топоса с классификатором подобъектов Ω и универсальной стрелкой true: 1→Ω (classifying map).  
   Репо: pkg/sem/, docs/sem_soundness.md, docs/limits.md.  
   Нетворкинг: первый пост — GitHub-репозиторий, просим ревью по η-формам и натуральности curry/uncurry.

6) Неделя 6 — универсальное свойство и критические пары  
   Инженерная ветка: задай генераторы Σ и универсальное свойство IR: “для любой CCC 𝒟 ∃! ⟦–⟧: IR→𝒟”; дополни все критические пары (β×, η×, β→, η→, compose/pair, compose/πᵢ, assoc, eval-context, unit, η× после β×, curry/uncurry); pkg/ir/equal.go.  
   Теоретическая ветка: Кан-расширения (Mac Lane X.3) — общий шаблон “расширения смысла”; Free-CCC = левый сопряжённый, реализуемый через Кан-экстенсию.  
   Репо: docs/free_ccc.md, tests/equal_critical_test.go.  
   Нетворкинг: второй пост — ревью универсального свойства и диаграмм натуральности.

7) Неделя 7 — упаковка и мост ко второму циклу  
   Инженерная ветка: README с путём Term (AST+типизация+контексты) → F → IR (free-CCC+NbE+NF-уникальность) → Sem (FinSet, экстенсиональная проверка); Graphviz-диаграммы всех морфизмов.  
   Теоретическая ветка: введение в топосы (Johnstone “Elephant” §A1.2): топос = CCC + классификатор подобъектов; FinSet — частный случай; мост к sheaf/presheaf моделям для следующего цикла.  
   Репо: README.md, graphviz/*.gv, docs/topos_intro.md.  
   Нетворкинг: публикация репо и точечные письма авторам (Heunen, Jacobs) с вопросами про NbE, Yoneda, free-CCC, натуральность curry/uncurry.